namespace AlgorithmsVisualiser.Helpers
{
    /// <summary>
    /// This enum contains different orders that the element can be in
    /// </summary>
    public enum EOrder
    {
        ASCENDING,
        DESCENDING,
        RANDOM
    }
}
